#include <stdio.h>

/*打印华氏度-摄氏度对照表*/

main()
{
	int fahr;
	/*
	fahr = 0,初始化部分，仅在循环开始执行一次
	fahr <= 300，循环控制部分，如果为真，则执行循环体
	fahr = fahr + 20，摄氏度+20 
	*/
	for (fahr = 0; fahr <= 300; fahr = fahr + 20)
		printf("%3d %6.1f\n", fahr, (5.0/9.0)*(fahr - 32));
 } 
